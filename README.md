# Article Search

A simple React project that lets users search through a list of articles.

As the user types in the search box, the matching articles are filtered and the searched text is highlighted. The search can also be cleared using the button inside the input field.

## Run the project

```bash
npm install
npm run dev
```

Then open the local link shown in the terminal.

## Built with

- React
- Vite
- CSS
